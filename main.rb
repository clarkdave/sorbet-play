# typed: true

# 1. this should show documentation on hover (from the `sorbet-typed` defs) but
#    it doesn't
#
# 2. "goto definition" goes to the auto-generated types in `rbi/gems` instead of
#    those in `rbi/sorbet-types/lib/bencode`
#
# if you comment out line 12 in `rbi/gems/bencode.rbi` then the documentation
# will show up as expected

[].bencode
